services:
  redis:
    image: redis:alpine
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    restart: unless-stopped
  # worker:
  #   build: ./backend
  #   command: celery -A app.core.celery_app worker --loglevel=info
  #   depends_on:
  #     - redis

volumes:
  redis_data:
